import { baseColors, typography, spacing } from './theme';

const lightTheme = {
  colors: {
    ...baseColors,
    // You can override specific colors for light theme here
  },
  typography,
  spacing,
};

export default lightTheme;